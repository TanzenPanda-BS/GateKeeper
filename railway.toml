[build]
builder = "NIXPACKS"
buildCommand = "npm ci && ./node_modules/.bin/tsx script/build.ts"

[deploy]
startCommand = "node dist/index.cjs"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
